# Author: JD 02/16/2022
# The first tuple is named as weekdays, the second is weekends
weekdays = ("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

weekends = ("Saturday", "Sunday")
# The third is named as days of the week, and it is the "sum" of the first two variables.
days_of_the_week = weekdays + weekends
# Printing the third variable by using a print() statement.
print(days_of_the_week)
